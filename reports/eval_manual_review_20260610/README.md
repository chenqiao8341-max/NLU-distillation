# Eval Manual Review 20260610

This directory snapshots the five manually reviewed eval datasets from:

```text
/home/qiao/work/nlu-server/eval/
```

Files:

- `data_type.json`
- `作者和机构.json`
- `机构和国家.json`
- `时间和if值.json`
- `topic和keywords.json`

`nlu-server` is not a Git repository on this machine, so this snapshot keeps the reviewed eval data under version control in `NLU-distillation`.
