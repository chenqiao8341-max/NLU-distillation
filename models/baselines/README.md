# Baselines

Use this directory to track non-primary model routes:

- rule-based date and IF extraction
- original base model without SFT
- existing API teacher outputs
- large local teacher outputs for pseudo-label generation

Baselines are important because some NLU slots may be cheaper and more stable with rules than with a small model.
